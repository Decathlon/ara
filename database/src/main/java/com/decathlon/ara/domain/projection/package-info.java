/**
 * The result of one or several entities fields, or field aggregation (min/max/count/...), queried from database and
 * (possibly joined) returned as one Java object by repository methods.
 */
package com.decathlon.ara.domain.projection;
